# Figma API Configuration
# Get your token from: https://www.figma.com/settings -> Personal access tokens
FIGMA_API_KEY=your-figma-api-token-here

# Optional: OAuth token (if using OAuth instead of Personal Access Token)
# FIGMA_OAUTH_TOKEN=your-oauth-token-here

# Optional: Server configuration
# PORT=3333
# OUTPUT_FORMAT=yaml
# SKIP_IMAGE_DOWNLOADS=false
EOF < /dev/null